{%- liquid
  assign featured_product = all_products[section.settings.product]
  assign product_url = routes.all_products_collection_url
  if featured_product != blank
    assign product_url = featured_product.url
  endif
-%}

<section class="section" data-section-id="{{ section.id }}">
  <div class="page-width featured-product">
    <div class="featured-product__image">
      {% if featured_product.featured_media %}
        {{
          featured_product.featured_media.preview_image
          | image_url: width: 1200
          | image_tag:
            widths: '480, 720, 960, 1200',
            sizes: '(min-width: 960px) 42vw, 92vw',
            loading: 'lazy',
            alt: featured_product.featured_media.alt | default: featured_product.title
        }}
      {% else %}
        <img src="{{ 'yemeni-corner-bag.svg' | asset_url }}" alt="Yemeni Corner Signature Roast coffee bag" loading="lazy">
      {% endif %}
    </div>

    <div class="featured-product__content">
      <p class="eyebrow">{{ section.settings.eyebrow }}</p>
      <h2 class="heading-lg">
        {% if featured_product != blank %}
          {{ featured_product.title }}
        {% else %}
          {{ section.settings.heading }}
        {% endif %}
      </h2>
      <p class="lead">{{ section.settings.text }}</p>

      <div class="product-points">
        <div class="point">
          <span>Tasting Notes</span>
          <strong>Warm spice, cocoa, dried fruit</strong>
        </div>
        <div class="point">
          <span>Roast Level</span>
          <strong>Medium, smooth, aromatic</strong>
        </div>
        <div class="point">
          <span>Best For</span>
          <strong>Drip, French press, pour-over</strong>
        </div>
      </div>

      <div class="button-row">
        <a class="button" href="{{ product_url }}">{{ section.settings.button_label }}</a>
        <a class="button button--secondary" href="/pages/story">Why Yemeni Coffee</a>
      </div>
    </div>
  </div>
</section>

{% schema %}
{
  "name": "Featured signature product",
  "settings": [
    {
      "type": "product",
      "id": "product",
      "label": "Product"
    },
    {
      "type": "text",
      "id": "eyebrow",
      "label": "Eyebrow",
      "default": "Signature Roast"
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Fallback heading",
      "default": "Yemeni Corner Signature Roast"
    },
    {
      "type": "textarea",
      "id": "text",
      "label": "Text",
      "default": "Premium Yemeni coffee, roasted for a rich, smooth, and memorable cup. One simple launch product built around heritage, quality, and everyday ritual."
    },
    {
      "type": "text",
      "id": "button_label",
      "label": "Button label",
      "default": "Shop Signature Roast"
    }
  ],
  "presets": [
    {
      "name": "Featured signature product"
    }
  ]
}
{% endschema %}
